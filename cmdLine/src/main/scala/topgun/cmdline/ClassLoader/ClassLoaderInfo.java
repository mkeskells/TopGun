package topgun.cmdline.ClassLoader;

import com.sun.jdi.Value;
import jdk.jfr.consumer.RecordedClass;

import java.net.URLClassLoader;
import java.util.HashMap;
import java.util.Map;

public class ClassLoaderInfo {
//    private final URLClassLoader classLoader;
//    private final Map<String, ClassInfo> allClasses = new HashMap<>();
//    public ClassLoaderInfo(URLClassLoader classLoader) {
//        this.classLoader = classLoader;
//    }
//
//    public ClassInfo lookup(RecordedClass clazz){
//        if(!allClasses.containsKey(clazz.getName())){
//            ClassInfo newClassInfo = new ClassInfo(clazz.getName(), );
//            allClasses.put(clazz.getName(), newClassInfo);
//        }
//
//        return allClasses.get(clazz.getName());
//
//    }
}
